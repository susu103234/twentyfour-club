node_modules
dist
dist-ssr
*.tsbuildinfo
.DS_Store
*.local
.vscode/*
!.vscode/extensions.json

# Tauri
src-tauri/target
src-tauri/gen
# Cargo.lock is committed on purpose — reproducible release builds need a
# pinned dep graph. Do NOT re-add it to ignores.
